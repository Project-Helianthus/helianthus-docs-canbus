name: documentation

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/check_docs.sh
      - run: ./scripts/test_docs_gate.sh
      - run: python3 scripts/validate_gree_protocols.py
      - run: ./scripts/test_gree_protocols.sh
